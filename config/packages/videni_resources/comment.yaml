App\Enity\Comment:
    attributes:
        denormalization_context:
            groups: [write]
        normalization_context:
            groups: [read]
        route_prefix: /admin
        validation_groups: [pintushi]
    collectionOperations:
        create_comment:
            denormalization_context:
                groups: [write]
            factory: '@app_factory_comment->create($postId)'
            methods: [POST]
            normalization_context:
                groups: [read]
            path: /comments/{postId}
            validation_groups: [pintushi]
        get_comments:
            grid: app_comment_grid
            methods: [GET]
            path: /comments
    itemOperations:
        delete_comment:
            methods: [GET]
            path: /comments/{id}
        update_comment:
            denormalization_context:
                groups: [write]
            methods: [PUT]
            normalization_context:
                groups: [read]
            path: /comments
            validation_groups: [pintushi]
